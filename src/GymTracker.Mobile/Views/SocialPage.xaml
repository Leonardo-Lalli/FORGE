<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:GymTracker.Mobile.ViewModels"
    x:Class="GymTracker.Mobile.Views.SocialPage"
    x:DataType="vm:SocialViewModel"
    Title="Social"
    Shell.NavBarIsVisible="False">

    <Grid RowDefinitions="Auto,*" BackgroundColor="{StaticResource Surface}">

        <!-- Title + Tab Chips -->
        <VerticalStackLayout Grid.Row="0" Padding="20,16,20,4" Spacing="10">
            <Label Text="Classifica"
                   FontFamily="LexendBold" FontSize="22" FontAttributes="Bold"
                   TextColor="{StaticResource OnSurface}" />
            <HorizontalStackLayout Spacing="8">
                <Border BackgroundColor="{StaticResource LimeGreen}"
                        StrokeShape="RoundRectangle 16" Padding="16,8">
                    <Label Text="VOLUME"
                           FontFamily="InterBold" FontSize="12" FontAttributes="Bold"
                           TextColor="{StaticResource OnSecondary}" />
                </Border>
                <Border BackgroundColor="{StaticResource SurfaceContainer}"
                        StrokeShape="RoundRectangle 16" Padding="16,8">
                    <Label Text="MASSIMALE"
                           FontFamily="InterBold" FontSize="12" FontAttributes="Bold"
                           TextColor="{StaticResource TextSecondary}" />
                </Border>
                <Border BackgroundColor="{StaticResource SurfaceContainer}"
                        StrokeShape="RoundRectangle 16" Padding="16,8">
                    <Label Text="XP"
                           FontFamily="InterBold" FontSize="12" FontAttributes="Bold"
                           TextColor="{StaticResource TextSecondary}" />
                </Border>
            </HorizontalStackLayout>
        </VerticalStackLayout>

        <!-- Compact Ranking List -->
        <ScrollView Grid.Row="1" Padding="20,0,20,100">
            <VerticalStackLayout Spacing="6">

                <!-- #1 Sofia -->
                <Border Style="{StaticResource CardFrame}" BackgroundColor="{StaticResource SurfaceContainerHigh}"
                        Padding="14,12">
                    <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                        <Border Grid.Column="0" BackgroundColor="{StaticResource SurfaceContainer}"
                                StrokeShape="RoundRectangle 8" WidthRequest="36" HeightRequest="36" Padding="0">
                            <Label Text="1" FontFamily="LexendBold" FontSize="16"
                                   TextColor="{StaticResource LimeGreen}"
                                   HorizontalOptions="Center" VerticalOptions="Center" />
                        </Border>
                        <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="1">
                            <Label Text="Sofia" FontFamily="LexendBold" FontSize="14" FontAttributes="Bold"
                                   TextColor="{StaticResource OnSurface}" />
                            <Label Text="Prima in classifica" FontFamily="InterRegular" FontSize="11"
                                   TextColor="{StaticResource LimeGreen}" />
                        </VerticalStackLayout>
                        <Label Grid.Column="2" Text="16.850 kg"
                               FontFamily="LexendBold" FontSize="16" FontAttributes="Bold"
                               TextColor="{StaticResource Primary}" VerticalOptions="Center" />
                    </Grid>
                </Border>

                <!-- #2 Marco (you) -->
                <Border Style="{StaticResource CardFrame}" BackgroundColor="{StaticResource PrimaryContainer}"
                        Stroke="{StaticResource Primary}" StrokeThickness="1.5" Padding="14,12">
                    <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                        <Border Grid.Column="0" BackgroundColor="{StaticResource Primary}"
                                StrokeShape="RoundRectangle 8" WidthRequest="36" HeightRequest="36" Padding="0">
                            <Label Text="2" FontFamily="LexendBold" FontSize="16"
                                   TextColor="{StaticResource OnPrimary}"
                                   HorizontalOptions="Center" VerticalOptions="Center" />
                        </Border>
                        <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="1">
                            <Label Text="Tu · Marco" FontFamily="LexendBold" FontSize="14" FontAttributes="Bold"
                                   TextColor="{StaticResource OnSurface}" />
                            <Label Text="2° posto" FontFamily="InterRegular" FontSize="11"
                                   TextColor="{StaticResource TextSecondary}" />
                        </VerticalStackLayout>
                        <Label Grid.Column="2" Text="14.100 kg"
                               FontFamily="LexendBold" FontSize="16" FontAttributes="Bold"
                               TextColor="{StaticResource OnSurface}" VerticalOptions="Center" />
                    </Grid>
                </Border>

                <!-- #3 Luca -->
                <Border Style="{StaticResource CardFrame}" BackgroundColor="{StaticResource SurfaceContainerHigh}"
                        Padding="14,12">
                    <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                        <Border Grid.Column="0" BackgroundColor="{StaticResource SurfaceContainer}"
                                StrokeShape="RoundRectangle 8" WidthRequest="36" HeightRequest="36" Padding="0">
                            <Label Text="3" FontFamily="LexendBold" FontSize="16"
                                   TextColor="{StaticResource TextSecondary}"
                                   HorizontalOptions="Center" VerticalOptions="Center" />
                        </Border>
                        <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="1">
                            <Label Text="Luca" FontFamily="LexendBold" FontSize="14" FontAttributes="Bold"
                                   TextColor="{StaticResource OnSurface}" />
                            <Label Text="3° posto" FontFamily="InterRegular" FontSize="11"
                                   TextColor="{StaticResource TextSecondary}" />
                        </VerticalStackLayout>
                        <Label Grid.Column="2" Text="12.900 kg"
                               FontFamily="LexendBold" FontSize="16" FontAttributes="Bold"
                               TextColor="{StaticResource Primary}" VerticalOptions="Center" />
                    </Grid>
                </Border>

                <!-- #4 Andrea -->
                <Border Style="{StaticResource CardFrame}" BackgroundColor="{StaticResource SurfaceContainerHigh}"
                        Padding="14,12">
                    <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                        <Border Grid.Column="0" BackgroundColor="{StaticResource SurfaceContainer}"
                                StrokeShape="RoundRectangle 8" WidthRequest="36" HeightRequest="36" Padding="0">
                            <Label Text="4" FontFamily="LexendBold" FontSize="16"
                                   TextColor="{StaticResource TextSecondary}"
                                   HorizontalOptions="Center" VerticalOptions="Center" />
                        </Border>
                        <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="1">
                            <Label Text="Andrea" FontFamily="LexendBold" FontSize="14" FontAttributes="Bold"
                                   TextColor="{StaticResource OnSurface}" />
                            <Label Text="4° posto" FontFamily="InterRegular" FontSize="11"
                                   TextColor="{StaticResource TextSecondary}" />
                        </VerticalStackLayout>
                        <Label Grid.Column="2" Text="12.550 kg"
                               FontFamily="LexendBold" FontSize="16" FontAttributes="Bold"
                               TextColor="{StaticResource Primary}" VerticalOptions="Center" />
                    </Grid>
                </Border>

            </VerticalStackLayout>
        </ScrollView>

    </Grid>

</ContentPage>
