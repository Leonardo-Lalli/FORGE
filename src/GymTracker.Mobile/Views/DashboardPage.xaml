<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:GymTracker.Mobile.ViewModels"
    x:Class="GymTracker.Mobile.Views.DashboardPage"
    x:DataType="vm:DashboardViewModel"
    Title="Home"
    Shell.NavBarIsVisible="False">

    <Grid RowDefinitions="*" BackgroundColor="{DynamicResource Surface}">

        <ScrollView Padding="20,16,20,100">
            <VerticalStackLayout Spacing="16">

                <!-- Greeting + Bell -->
                <Grid ColumnDefinitions="*,Auto">
                    <VerticalStackLayout Grid.Column="0" Spacing="2">
                        <Label Text="Oggi" FontFamily="OpenSansRegular" FontSize="14" TextColor="{DynamicResource TextSecondary}" />
                        <Label Text="Ciao, IronAthlete" FontFamily="OpenSansSemibold" FontSize="26" FontAttributes="Bold"
                               TextColor="{DynamicResource OnSurface}" />
                    </VerticalStackLayout>
                    <Border Grid.Column="1" BackgroundColor="{DynamicResource SurfaceContainer}"
                            StrokeShape="RoundRectangle 12" Padding="8" HeightRequest="40" WidthRequest="40"
                            VerticalOptions="Center">
                        <Label Text="⬤" FontSize="14"
                               TextColor="{DynamicResource Primary}"
                               HorizontalOptions="Center" VerticalOptions="Center">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding OpenNotificationsCommand}" />
                        </Label.GestureRecognizers>
                    </Label>
                    </Border>
                </Grid>

                <!-- Streak + Rank -->
                <Border Style="{DynamicResource CardFrame}" BackgroundColor="{DynamicResource SurfaceContainerHigh}">
                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="16">
                        <VerticalStackLayout Grid.Column="0" VerticalOptions="Center" Spacing="2">
                            <Label Text="STREAK ATTIVO" FontFamily="OpenSansSemibold" FontSize="11" FontAttributes="Bold"
                                   TextColor="{DynamicResource TextSecondary}" CharacterSpacing="0.8" />
                            <Label Text="{Binding StreakText}" FontFamily="OpenSansSemibold" FontSize="28" FontAttributes="Bold"
                                   TextColor="{DynamicResource LimeGreen}" />
                        </VerticalStackLayout>
                        <Label Grid.Column="1" Text="{Binding LeaderboardPosition, StringFormat='#{0}'}"
                               FontFamily="OpenSansSemibold" FontSize="40" FontAttributes="Bold"
                               TextColor="{DynamicResource Primary}" VerticalOptions="Center" />
                    </Grid>
                </Border>

                <!-- Stats 2x2 -->
                <Grid ColumnDefinitions="*,12,*" RowDefinitions="Auto,12,Auto">
                    <Border Grid.Row="0" Grid.Column="0" Style="{DynamicResource CardFrame}">
                        <VerticalStackLayout Spacing="6">
                            <Label Text="PESO" FontFamily="OpenSansSemibold" FontSize="11" FontAttributes="Bold"
                                   TextColor="{DynamicResource TextSecondary}" CharacterSpacing="0.8" />
                            <Label Text="{Binding CurrentWeight}" FontFamily="OpenSansSemibold" FontSize="36" FontAttributes="Bold"
                                   TextColor="{DynamicResource OnSurface}" />
                            <Label Text="Ultima rilevazione" FontFamily="OpenSansRegular" FontSize="12"
                                   TextColor="{DynamicResource Outline}" />
                        </VerticalStackLayout>
                    </Border>
                    <Border Grid.Row="0" Grid.Column="2" Style="{DynamicResource CardFrame}">
                        <VerticalStackLayout Spacing="6">
                            <Label Text="CLASSIFICA" FontFamily="OpenSansSemibold" FontSize="11" FontAttributes="Bold"
                                   TextColor="{DynamicResource TextSecondary}" CharacterSpacing="0.8" />
                            <Label Text="{Binding LeaderboardPosition, StringFormat='#{0}'}"
                                   FontFamily="OpenSansSemibold" FontSize="36" FontAttributes="Bold"
                                   TextColor="{DynamicResource Primary}" />
                            <Label Text="Tra i tuoi amici" FontFamily="OpenSansRegular" FontSize="12"
                                   TextColor="{DynamicResource Outline}" />
                        </VerticalStackLayout>
                    </Border>
                    <Border Grid.Row="2" Grid.Column="0" Style="{DynamicResource CardFrame}">
                        <VerticalStackLayout Spacing="6">
                            <Label Text="VOLUME 7 GIORNI" FontFamily="OpenSansSemibold" FontSize="11" FontAttributes="Bold"
                                   TextColor="{DynamicResource TextSecondary}" CharacterSpacing="0.8" />
                            <Label Text="{Binding WeeklyVolume}" FontFamily="OpenSansSemibold" FontSize="36" FontAttributes="Bold"
                                   TextColor="{DynamicResource LimeGreen}" />
                            <Label Text="Peso x Ripetizioni" FontFamily="OpenSansRegular" FontSize="12"
                                   TextColor="{DynamicResource Outline}" />
                        </VerticalStackLayout>
                    </Border>
                    <Border Grid.Row="2" Grid.Column="2" Style="{DynamicResource CardFrame}">
                        <VerticalStackLayout Spacing="6">
                            <Label Text="ULTIMO ALLENAMENTO" FontFamily="OpenSansSemibold" FontSize="11" FontAttributes="Bold"
                                   TextColor="{DynamicResource TextSecondary}" CharacterSpacing="0.8" />
                            <Label Text="{Binding LastWorkoutSummary}" FontFamily="OpenSansRegular" FontSize="16"
                                   TextColor="{DynamicResource OnSurface}" />
                        </VerticalStackLayout>
                    </Border>
                </Grid>

                <!-- CTA -->
                <Grid ColumnDefinitions="*,12,*">
                    <Button Grid.Column="0" Text="ALLENAMENTO" Style="{DynamicResource PrimaryButton}" HeightRequest="56" FontSize="15" FontFamily="OpenSansSemibold" FontAttributes="Bold" />
                    <Button Grid.Column="2" Text="ESERCIZI" Style="{DynamicResource SecondaryButton}" HeightRequest="52" />
                </Grid>

                <!-- Friend Feed -->
                <Label Text="FEED AMICI" FontFamily="OpenSansSemibold" FontSize="11" FontAttributes="Bold"
                       TextColor="{DynamicResource TextSecondary}" CharacterSpacing="0.8" Margin="0,4,0,0" />

                <!-- Friend 1: Sofia -->
                <Border Style="{DynamicResource CardFrame}" BackgroundColor="{DynamicResource SurfaceContainerHigh}">
                    <VerticalStackLayout Spacing="10">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="10">
                            <Border Grid.Column="0" BackgroundColor="{DynamicResource PrimaryContainer}"
                                    StrokeShape="RoundRectangle 20" WidthRequest="40" HeightRequest="40" Padding="0">
                                <Label Text="S" FontFamily="OpenSansSemibold" FontSize="16"
                                       TextColor="{DynamicResource OnPrimaryContainer}"
                                       HorizontalOptions="Center" VerticalOptions="Center" />
                            </Border>
                            <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="1">
                                <Label Text="Sofia" FontFamily="OpenSansSemibold" FontSize="14" FontAttributes="Bold"
                                       TextColor="{DynamicResource OnSurface}" />
                                <Label Text="Oggi, 08:30 · Streak 🔥 12" FontFamily="OpenSansRegular" FontSize="11"
                                       TextColor="{DynamicResource LimeGreen}" />
                            </VerticalStackLayout>
                            <Label Grid.Column="2" Text="👑" FontSize="20" VerticalOptions="Center" />
                        </Grid>
                        <Label Text="Panca Piana 4×10@80kg · Squat 4×8@100kg · Stacchi 3×6@130kg"
                               FontFamily="OpenSansRegular" FontSize="13" TextColor="{DynamicResource TextSecondary}" />
                        <Label Text="4,200 kg" FontFamily="OpenSansSemibold" FontSize="20" FontAttributes="Bold"
                               TextColor="{DynamicResource ElectricBlue}" />

                        <!-- Comments -->
                        <Border BackgroundColor="{DynamicResource SurfaceContainer}" StrokeShape="RoundRectangle 8" Padding="12,10">
                            <VerticalStackLayout Spacing="6">
                                <HorizontalStackLayout Spacing="8">
                                    <Border BackgroundColor="{DynamicResource PrimaryContainer}" StrokeShape="RoundRectangle 10" WidthRequest="24" HeightRequest="24" Padding="0">
                                        <Label Text="M" FontFamily="OpenSansSemibold" FontSize="10" TextColor="{DynamicResource OnPrimaryContainer}"
                                               HorizontalOptions="Center" VerticalOptions="Center" />
                                    </Border>
                                    <VerticalStackLayout Spacing="2">
                                        <HorizontalStackLayout Spacing="6">
                                            <Label Text="Marco" FontFamily="OpenSansSemibold" FontSize="11" TextColor="{DynamicResource Primary}" />
                                            <Label Text="2m fa" FontFamily="OpenSansRegular" FontSize="10" TextColor="{DynamicResource TextSecondary}" />
                                        </HorizontalStackLayout>
                                        <Label Text="Ma sei un mostro! 💪 Quei 100kg di squat fanno paura" FontFamily="OpenSansRegular" FontSize="12"
                                               TextColor="{DynamicResource OnSurface}" />
                                    </VerticalStackLayout>
                                </HorizontalStackLayout>
                                <BoxView HeightRequest="0.5" Color="{DynamicResource OutlineVariant}" />
                                <HorizontalStackLayout Spacing="8">
                                    <Border BackgroundColor="{DynamicResource SurfaceContainerHigh}" StrokeShape="RoundRectangle 10" WidthRequest="24" HeightRequest="24" Padding="0">
                                        <Label Text="S" FontFamily="OpenSansSemibold" FontSize="10" TextColor="{DynamicResource LimeGreen}"
                                               HorizontalOptions="Center" VerticalOptions="Center" />
                                    </Border>
                                    <VerticalStackLayout Spacing="2">
                                        <HorizontalStackLayout Spacing="6">
                                            <Label Text="Sofia" FontFamily="OpenSansSemibold" FontSize="11" TextColor="{DynamicResource LimeGreen}" />
                                            <Label Text="1m fa" FontFamily="OpenSansRegular" FontSize="10" TextColor="{DynamicResource TextSecondary}" />
                                        </HorizontalStackLayout>
                                        <Label Text="Grazie! Tu però mi stai raggiungendo... #4 non è male 😉" FontFamily="OpenSansRegular" FontSize="12"
                                               TextColor="{DynamicResource OnSurface}" />
                                    </VerticalStackLayout>
                                </HorizontalStackLayout>
                            </VerticalStackLayout>
                        </Border>
                        <Label Text="🔥 Sfida · 💬 Commenta" FontFamily="OpenSansSemibold" FontSize="11"
                               TextColor="{DynamicResource Primary}" />
                    </VerticalStackLayout>
                </Border>

                <!-- Friend 2: Luca -->
                <Border Style="{DynamicResource CardFrame}" BackgroundColor="{DynamicResource SurfaceContainerHigh}">
                    <VerticalStackLayout Spacing="10">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="10">
                            <Border Grid.Column="0" BackgroundColor="{DynamicResource SurfaceContainer}"
                                    StrokeShape="RoundRectangle 20" WidthRequest="40" HeightRequest="40" Padding="0">
                                <Label Text="L" FontFamily="OpenSansSemibold" FontSize="16"
                                       TextColor="{DynamicResource TextSecondary}"
                                       HorizontalOptions="Center" VerticalOptions="Center" />
                            </Border>
                            <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="1">
                                <Label Text="Luca" FontFamily="OpenSansSemibold" FontSize="14" FontAttributes="Bold"
                                       TextColor="{DynamicResource OnSurface}" />
                                <Label Text="Ieri, 19:00 · Streak 🔥 8" FontFamily="OpenSansRegular" FontSize="11"
                                       TextColor="{DynamicResource TextSecondary}" />
                            </VerticalStackLayout>
                        </Grid>
                        <Label Text="Shoulder Press 3×12@30kg · Lat Machine 4×10@65kg · Curl 3×12@16kg"
                               FontFamily="OpenSansRegular" FontSize="13" TextColor="{DynamicResource TextSecondary}" />
                        <Label Text="2,100 kg" FontFamily="OpenSansSemibold" FontSize="20" FontAttributes="Bold"
                               TextColor="{DynamicResource Primary}" />
                        <Border BackgroundColor="{DynamicResource SurfaceContainer}" StrokeShape="RoundRectangle 8" Padding="12,10">
                            <HorizontalStackLayout Spacing="8">
                                <Border BackgroundColor="{DynamicResource PrimaryContainer}" StrokeShape="RoundRectangle 10" WidthRequest="24" HeightRequest="24" Padding="0">
                                    <Label Text="M" FontFamily="OpenSansSemibold" FontSize="10" TextColor="{DynamicResource OnPrimaryContainer}"
                                           HorizontalOptions="Center" VerticalOptions="Center" />
                                </Border>
                                <VerticalStackLayout Spacing="2">
                                    <HorizontalStackLayout Spacing="6">
                                        <Label Text="Tu" FontFamily="OpenSansSemibold" FontSize="11" TextColor="{DynamicResource Primary}" />
                                        <Label Text="5h fa" FontFamily="OpenSansRegular" FontSize="10" TextColor="{DynamicResource TextSecondary}" />
                                    </HorizontalStackLayout>
                                    <Label Text="Bella Luca! Domani spalle anche io, ti raggiungo 💪" FontFamily="OpenSansRegular" FontSize="12"
                                           TextColor="{DynamicResource OnSurface}" />
                                </VerticalStackLayout>
                            </HorizontalStackLayout>
                        </Border>
                        <Label Text="🔥 Sfida · 💬 Commenta" FontFamily="OpenSansSemibold" FontSize="11"
                               TextColor="{DynamicResource Primary}" />
                    </VerticalStackLayout>
                </Border>

            </VerticalStackLayout>
        </ScrollView>
    </Grid>

</ContentPage>
