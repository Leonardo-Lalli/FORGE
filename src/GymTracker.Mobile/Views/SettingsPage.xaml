<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:GymTracker.Mobile.ViewModels"
    x:Class="GymTracker.Mobile.Views.SettingsPage"
    x:DataType="vm:SettingsViewModel"
    Title="Settings"
    Shell.NavBarIsVisible="False">

    <Grid RowDefinitions="Auto,*" BackgroundColor="{DynamicResource Surface}">

        <!-- Header -->
        <Grid Grid.Row="0" ColumnDefinitions="Auto,*,Auto" Padding="24,20">
            <Border Grid.Column="0" BackgroundColor="{DynamicResource SurfaceContainer}"
                    StrokeShape="RoundRectangle 999" WidthRequest="40" HeightRequest="40" Padding="0"
                    Stroke="{DynamicResource OutlineVariant}" StrokeThickness="0.5">
                <Label Text="←" FontSize="18" TextColor="{DynamicResource OnSurface}"
                       HorizontalOptions="Center" VerticalOptions="Center">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding GoBackCommand}" />
                    </Label.GestureRecognizers>
                </Label>
            </Border>
            <Label Grid.Column="1" Text="Impostazioni"
                   FontFamily="OpenSansSemibold" FontSize="18" FontAttributes="Bold"
                   TextColor="{DynamicResource OnSurface}"
                   HorizontalOptions="Center" VerticalOptions="Center" />
        </Grid>

        <ScrollView Grid.Row="1" Padding="24,8,24,100">
            <VerticalStackLayout Spacing="16">

                <!-- Appearance Section -->
                <Label Text="ASPETTO"
                       FontFamily="OpenSansSemibold" FontSize="11" FontAttributes="Bold"
                       TextColor="{DynamicResource TextSecondary}"
                       CharacterSpacing="0.8" Margin="0,8,0,0" />

                <!-- Dark Mode Toggle -->
                <Border BackgroundColor="{DynamicResource SurfaceContainerHigh}"
                        StrokeShape="RoundRectangle 12"
                        Stroke="{DynamicResource OutlineVariant}" StrokeThickness="1"
                        Padding="16">
                    <Grid ColumnDefinitions="*,Auto">
                        <VerticalStackLayout Grid.Column="0" VerticalOptions="Center" Spacing="4">
                            <Label Text="Modalità Scura"
                                   FontFamily="OpenSansSemibold" FontSize="16" FontAttributes="Bold"
                                   TextColor="{DynamicResource OnSurface}" />
                            <Label Text="Attiva il tema Cyber-Athletic Elite"
                                   FontFamily="OpenSansRegular" FontSize="13"
                                   TextColor="{DynamicResource TextSecondary}" />
                        </VerticalStackLayout>
                        <Switch Grid.Column="1" IsToggled="{Binding IsDarkMode}"
                                OnColor="{DynamicResource Primary}"
                                VerticalOptions="Center" />
                    </Grid>
                </Border>

                <!-- Theme Preview -->
                <Border BackgroundColor="{DynamicResource SurfaceContainerLowest}"
                        StrokeShape="RoundRectangle 12"
                        Stroke="{DynamicResource OutlineVariant}" StrokeThickness="1"
                        Padding="20">
                    <VerticalStackLayout Spacing="12" HorizontalOptions="Center">
                        <Label Text="Anteprima Tema Attuale"
                               FontFamily="OpenSansSemibold" FontSize="11"
                               TextColor="{DynamicResource TextSecondary}"
                               CharacterSpacing="0.8" HorizontalTextAlignment="Center" />
                        <Grid ColumnDefinitions="48,*,48" ColumnSpacing="12">
                            <BoxView Grid.Column="0" Color="{DynamicResource Primary}"
                                     CornerRadius="24" WidthRequest="48" HeightRequest="48" />
                            <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="4">
                                <BoxView Color="{DynamicResource OnSurface}"
                                         HeightRequest="12" CornerRadius="6" />
                                <BoxView Color="{DynamicResource TextSecondary}"
                                         HeightRequest="8" CornerRadius="4"
                                         HorizontalOptions="Start" WidthRequest="120" />
                            </VerticalStackLayout>
                            <BoxView Grid.Column="2" Color="{DynamicResource SecondaryContainer}"
                                     CornerRadius="24" WidthRequest="48" HeightRequest="48" />
                        </Grid>
                    </VerticalStackLayout>
                </Border>

                <!-- Tema Chiaro Preview -->
                <Label Text="TEMI DISPONIBILI"
                       FontFamily="OpenSansSemibold" FontSize="11" FontAttributes="Bold"
                       TextColor="{DynamicResource TextSecondary}"
                       CharacterSpacing="0.8" Margin="0,8,0,0" />

                <Border BackgroundColor="{DynamicResource SurfaceContainerHigh}"
                        StrokeShape="RoundRectangle 12"
                        Stroke="{DynamicResource OutlineVariant}" StrokeThickness="1"
                        Padding="16">
                    <HorizontalStackLayout Spacing="16">
                        <Border BackgroundColor="#003ec7"
                                StrokeShape="RoundRectangle 16"
                                WidthRequest="56" HeightRequest="56" Padding="0"
                                Stroke="#003ec7" StrokeThickness="2" />
                        <VerticalStackLayout VerticalOptions="Center" Spacing="2">
                            <Label Text="Fitness Core" FontFamily="OpenSansSemibold" FontSize="14"
                                   FontAttributes="Bold" TextColor="{DynamicResource OnSurface}" />
                            <Label Text="Tema chiaro — blu professionale"
                                   FontFamily="OpenSansRegular" FontSize="12"
                                   TextColor="{DynamicResource TextSecondary}" />
                        </VerticalStackLayout>
                    </HorizontalStackLayout>
                </Border>

                <Border BackgroundColor="{DynamicResource SurfaceContainerHigh}"
                        StrokeShape="RoundRectangle 12"
                        Stroke="{DynamicResource OutlineVariant}" StrokeThickness="1"
                        Padding="16">
                    <HorizontalStackLayout Spacing="16">
                        <Border BackgroundColor="#c3f5ff"
                                StrokeShape="RoundRectangle 16"
                                WidthRequest="56" HeightRequest="56" Padding="0"
                                Stroke="#c3f5ff" StrokeThickness="2" />
                        <VerticalStackLayout VerticalOptions="Center" Spacing="2">
                            <Label Text="Cyber-Athletic Elite" FontFamily="OpenSansSemibold" FontSize="14"
                                   FontAttributes="Bold" TextColor="{DynamicResource OnSurface}" />
                            <Label Text="Tema scuro — ciano high-tech"
                                   FontFamily="OpenSansRegular" FontSize="12"
                                   TextColor="{DynamicResource TextSecondary}" />
                        </VerticalStackLayout>
                    </HorizontalStackLayout>
                </Border>

            </VerticalStackLayout>
        </ScrollView>
    </Grid>

</ContentPage>
